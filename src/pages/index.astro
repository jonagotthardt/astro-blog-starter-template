---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
import { plugins } from './plugins';
---

<html lang="en">
  <head>
    <BaseHead title="Jona Gotthardt • Plugin Developer" description={SITE_DESCRIPTION} />
    <style>
      header.hero {
        text-align: center;
        padding: 90px 20px 40px;
      }
      h1 {
        font-size: 3.2rem;
        margin: 0;
        background: linear-gradient(90deg, #00ff9c, #00bcd4);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        animation: shimmer 6s infinite linear;
        background-size: 200% auto;
      }
      @keyframes shimmer {
        0% { background-position: 0% center; }
        100% { background-position: 200% center; }
      }
      p.subtitle {
        font-size: 1.2rem;
        opacity: 0.85;
        margin-top: 12px;
      }

      section.about {
        max-width: 800px;
        margin: 50px auto;
        padding: 0 20px;
        text-align: center;
        line-height: 1.6;
        opacity: 0.9;
      }
      section.about strong {
        color: #00bcd4;
      }

      h2 {
        text-align: center;
        margin-top: 80px;
        font-size: 2rem;
      }
      .grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 25px;
        margin: 40px auto 10px;
        max-width: 1100px;
        padding: 0 20px;
      }
      .card {
        background: rgba(0, 0, 0, 0.4);
        border-radius: 12px;
        padding: 20px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.25);
        backdrop-filter: blur(6px);
        transition: transform 0.25s ease, background 0.25s ease;
      }
      .card:hover {
        transform: translateY(-5px);
        background: rgba(0, 0, 0, 0.55);
      }
      .card h3 {
        font-size: 1.5rem;
        margin: 0 0 8px;
      }
      .card p {
        opacity: 0.85;
        font-size: 1rem;
        margin-bottom: 10px;
      }
      .features {
        list-style: disc;
        margin: 0.5rem 0 1rem 1.2rem;
        opacity: 0.9;
        font-size: 0.95rem;
      }
      .meta {
        font-size: 0.9rem;
        opacity: 0.8;
      }
      .links a {
        display: inline-block;
        margin-right: 8px;
        padding: 7px 13px;
        border-radius: 6px;
        font-weight: 600;
        text-decoration: none;
        transition: 0.2s;
      }
      .btn {
        background: #00bcd4;
        color: white;
      }
      .btn:hover {
        background: #0097a7;
      }
      .ghost {
        border: 1px solid #00bcd4;
        color: #00bcd4;
      }
      .ghost:hover {
        background: #00bcd4;
        color: white;
      }
      .viewall {
        text-align: center;
        margin-top: 40px;
      }
      .viewall a {
        color: #00bcd4;
        font-weight: 600;
        text-decoration: none;
        transition: color 0.3s;
      }
      .viewall a:hover {
        color: #00e5ff;
      }

      footer {
        margin-top: 70px;
      }
    </style>
  </head>

  <body>
    <Header />

    <header class="hero">
      <h1>Jona Gotthardt</h1>
      <p class="subtitle">Plugin Developer • Creator • Innovator</p>
    </header>

    <section class="about">
      <p>
        Hey! I’m <strong>Jona Gotthardt</strong>, a passionate developer who builds custom Minecraft
        plugins and creative systems designed for both <strong>Java</strong> and <strong>Bedrock</strong>.
      </p>
      <p>
        My goal is to make plugins that are <strong>lightweight</strong>, <strong>highly optimized</strong>,
        and fun to use — always with quality, balance, and creativity in mind.
      </p>
    </section>

    <section>
      <h2>Latest Projects</h2>
      <div class="grid">
        {plugins.slice(0, 3).map((p) => (
          <div class="card">
            <h3>{p.title}</h3>
            <p>{p.short}</p>
            <ul class="features">
              {p.features.slice(0, 3).map((f) => <li>{f}</li>)}
            </ul>
            <p class="meta">Version {p.version} • MC {p.mcVersions.join(', ')}</p>
            <div class="links">
              {p.links.github && <a href={p.links.github} class="ghost" target="_blank">GitHub</a>}
              {p.links.modrinth && <a href={p.links.modrinth} class="ghost" target="_blank">Modrinth</a>}
              <a href="/plugins" class="btn">View Project</a>
            </div>
          </div>
        ))}
      </div>

      <div class="viewall">
        <a href="/plugins">→ View all plugins</a>
      </div>
    </section>

    <section class="about">
      <h2>What’s Next</h2>
      <p>
        I’m working on new creative ideas for 1.21.9+ — including utility packs, server enhancements,
        and unique gameplay mechanics that push Minecraft’s limits even further.
      </p>
      <p>
        Follow along to see what’s coming next. Every project is built to inspire creativity and fun.
      </p>
    </section>

    <Footer />
  </body>
</html>