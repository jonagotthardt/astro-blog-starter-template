---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
import { plugins } from '../data/plugins';
---

<html lang="en">
  <head>
    <BaseHead title="Jona's Plugins" description={SITE_DESCRIPTION} />
    <style>
      header.hero {
        padding: 80px 20px 40px;
        text-align: center;
      }
      h1 {
        font-size: 3.2rem;
        margin: 0;
        background: linear-gradient(90deg, #00ff9c, #00bcd4);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
      }
      p.subtitle {
        font-size: 1.2rem;
        opacity: 0.85;
        margin-top: 10px;
      }
      section {
        max-width: 1100px;
        margin: 0 auto;
        padding: 20px;
      }
      h2 {
        text-align: center;
        margin-top: 60px;
        font-size: 2rem;
      }
      .grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 25px;
        margin-top: 30px;
      }
      .card {
        background: rgba(0, 0, 0, 0.4);
        border-radius: 12px;
        padding: 20px;
        transition: 0.25s ease;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.25);
        backdrop-filter: blur(6px);
      }
      .card:hover {
        transform: translateY(-4px);
        background: rgba(0, 0, 0, 0.55);
      }
      .card h3 {
        margin-top: 0;
        font-size: 1.5rem;
      }
      .card p {
        opacity: 0.85;
        font-size: 1rem;
        margin: 0.4rem 0 0.8rem;
      }
      .features {
        list-style: disc;
        margin: 0.5rem 0 1rem 1.2rem;
        font-size: 0.95rem;
        opacity: 0.9;
      }
      .meta {
        font-size: 0.9rem;
        opacity: 0.8;
      }
      .links a {
        display: inline-block;
        margin-right: 8px;
        padding: 7px 13px;
        border-radius: 6px;
        font-weight: 600;
        text-decoration: none;
        transition: 0.2s;
      }
      .btn {
        background: #00bcd4;
        color: white;
      }
      .btn:hover {
        background: #0097a7;
      }
      .ghost {
        border: 1px solid #00bcd4;
        color: #00bcd4;
      }
      .ghost:hover {
        background: #00bcd4;
        color: white;
      }
      .about {
        max-width: 700px;
        margin: 50px auto 20px;
        text-align: center;
        opacity: 0.9;
      }
      footer {
        margin-top: 60px;
      }
    </style>
  </head>

  <body>
    <Header />

    <header class="hero">
      <h1>Jona’s Plugins</h1>
      <p class="subtitle">
        Creating lightweight, modern and performance-optimized plugins for Minecraft.
      </p>
    </header>

    <section class="about">
      <p>
        Welcome to my development hub! I’m <strong>Jona Gotthardt</strong> — a passionate
        plugin developer and designer focused on making creative, balanced, and
        well-optimized experiences for both Java and Bedrock players.
      </p>
      <p>
        Here you can explore my latest projects, see what I’m currently working on,
        and discover upcoming ideas. Everything is built with care and attention to detail.
      </p>
    </section>

    <section>
      <h2>My Projects</h2>
      <div class="grid">
        {plugins.map((p) => (
          <div class="card">
            <h3>{p.title}</h3>
            <p>{p.short}</p>
            <ul class="features">
              {p.features.slice(0, 3).map((f) => <li>{f}</li>)}
            </ul>
            <p class="meta">Version {p.version} • MC {p.mcVersions.join(', ')}</p>
            <div class="links">
              {p.links.github && <a href={p.links.github} class="ghost" target="_blank">GitHub</a>}
              {p.links.modrinth && <a href={p.links.modrinth} class="ghost" target="_blank">Modrinth</a>}
              {p.links.download && <a href={p.links.download} class="btn">Learn more</a>}
            </div>
          </div>
        ))}
      </div>
    </section>

    <section>
      <h2>What’s Next</h2>
      <p style="text-align:center; opacity:0.85;">
        I’m currently experimenting with new utility plugins and QoL tools for 1.21.9+.
        Stay tuned for something exciting — it’s just the beginning.
      </p>
    </section>

    <Footer />
  </body>
</html>