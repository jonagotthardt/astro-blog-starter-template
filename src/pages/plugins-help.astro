---
import Layout from "../layouts/BlogPost.astro";

const plugins = [
  {
    name: "AllMobEggs",
    version: "2.0",
    description: `
<p>AllMobEggs is a lightweight Paper plugin that adds <strong>custom crafting recipes</strong> for many vanilla spawn eggs.</p>

<ul>
<li>Adds <strong>fixed crafting recipes</strong> for multiple vanilla mobs</li>
<li>Recipes are <strong>automatically unlocked</strong> on player join</li>
<li>/eggrecipes command</li>
</ul>

<h3>Crafting Example</h3>
<pre><code> E 
 I 

E = Chicken Egg  
I = Rotten Flesh
</code></pre>

<h3>Installation</h3>
<ol>
<li>Download AllMobEggs.jar</li>
<li>Place it in /plugins</li>
<li>Restart server</li>
</ol>
`
  },
  {
    name: "MysticalGems",
    version: "1.0",
    description: `
<p>Magical gems with unique abilities for survival and RPG servers.</p>

<ul>
<li>Adds <strong>magical gems</strong> with custom powers</li>
<li>Gems can be used for crafting, potions, or as special items</li>
</ul>

<h3>Installation</h3>
<ol>
<li>Download MysticalGems.jar</li>
<li>Place it in /plugins</li>
<li>Restart server</li>
</ol>
`
  }
];
---

<style>
.plugin-readme {
  background-color: #1e1e1e; /* dunkler Hintergrund */
  color: #f5f5f5;           /* helle Schrift */
  padding: 1rem;
  border-radius: 8px;
  margin-bottom: 2rem;
  max-height: 600px;
  overflow-y: auto;
}
.plugin-readme pre {
  background-color: #1e1e2f; /* dunkle Codebox */
  color: #f8f8f2;
  padding: 1rem;
  border-radius: 6px;
  overflow-x: auto;
  font-family: 'Fira Code', monospace;
}
.plugin-readme code {
  background-color: #2d2d44; /* inline code */
  color: #f8f8f2;
  padding: 0.2rem 0.4rem;
  border-radius: 4px;
}
.plugin-readme strong { 
  background-color: #3a3a5c; /* hervorgehobene Wörter */
  padding: 0.1rem 0.3rem; 
  border-radius: 3px; 
}
</style>

<Layout title="Minecraft Plugin Help">
  <h1>Plugin Help</h1>
  {plugins.map(plugin => (
    <article>
      <h2>{plugin.name} <small>v{plugin.version}</small></h2>
      <div class="plugin-readme" innerHTML={plugin.description}></div>
    </article>
  ))}
</Layout>